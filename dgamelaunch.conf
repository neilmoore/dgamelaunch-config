# Global config variables:

# Max amount of registered users to allow.
maxusers = 64000

allow_new_nicks = yes

# Max length for newly registered nicks. Must be less than 20.
# By default, NetHack only stores the first 10 chars of a name into
# the record/logfile.
maxnicklen = 20

# Set the default watching-screen sorting mode. Can be one of
# "username", "game", "windowsize", "starttime" or "idletime".
# "username" is the default.
sortmode = "username"

# Maximum idle time in menus
menu_max_idle_time = 3600

# Path to a prepared chroot jail.
chroot_path = "%%DGL_CHROOT%%/"

# From inside the jail, dgamelaunch's working directory for rcfiles/ttyrec/etc
dglroot = "%%CHROOT_DGLDIR%%/"

# Server ID string
server_id = "CBRO - Dungeon Crawl Stone Soup - crawl.beRotato.org"


# From inside the jail, location of a banner file, the topmost line will be
# shown in submenus that cannot be defined separately.
# Some string substitution is done for the file:
# $VERSION   replaced with "dgamelaunch v" + dgl version number.
# $SERVERID  replaced with the server_id string, as defined above.
banner = "%%CHROOT_DGLDIR%%/data/dgl-banner"

# The following two options are fairly insecure. They will force us to 
# load the password/group database into memory while still having root
# privileges. Replace them with shed_uid/shed_gid entries as soon as
# possible if you decide to use them. dgamelaunch will inform you of
# the uids/gids corresponding to your choices when it loads.
#
# Note that shed_uid and shed_gid will always take precedence over
# shed_user and shed_group if they are specified.

# shed_user: username to shed privileges to
# shed_user = crawl
# shed_group: group name to shed privileges to
# shed_group = crawl

# Preferably, you may use the respective gids/uids.
# crawl/crawl
shed_uid = 1007
shed_gid = 1008

# The defaults are usually just fine for this. passwd refers to the file
# that stores the user database, and lockfile is only used internally by
# dgamelaunch.

passwd = "%%CHROOT_DGLDIR%%/data/dgl-login"
lockfile = "%%CHROOT_DGLDIR%%/data/dgl-lock"

watch_columns = [ ["", "", 0, "%s)"],
                  ["User", "username", 3, "%-15s"],
                  ["Game", "game", 19, "%-9s"],
                  ["Where", "extrainfo", 33, "%-18s"],
                  ["Term", "windowsize", 53, "%s"],
                  ["Idle", "idletime", 62, "%-10s"],
                  ["Viewers", "watchers", 73, "%4s"]
                ]

#
# define some commands that are run when something happens. format is
# commands [ <time> ] = <command> ...
#
# <time> can be one of:
#   dglstart  = when someone telnets in
#   login     = when user has logged in
#   register  = right after a new user is registered
#   gamestart = just before a game is started
#
# <command> is:
#   mkdir "foo"        = create a directory "foo"
#   chdir "foo"        = change current work dir to "foo"
#   cp "foo" "bar"     = copy file "foo" to "bar", overwriting previous "bar"
#   ifnxcp "foo" "bar" = copy file "foo" to "bar", if "bar" doesn't exist
#   unlink "foo"       = delete file "foo"
#   setenv "foo" "bar" = set environment variable "foo" to "bar"
#   exec "foo" "bar"   = execute "foo" with "bar" as it's param
#   chpasswd           = do the change password prompting, if logged in
#   chmail             = do the change email prompting, if logged in
#   watch_menu         = go to the watching menu
#   quit               = quit dgl
#   ask_login          = do the login prompting, if not logged in
#   ask_register       = do register new user prompting, if not logged in and
#                        registration of new nicks is allowed.
#   edit_options "foo" = edit options for game which has the short name "foo"
#                        (user must be logged in)
#   play_game "foo"    = start game which has the short name "foo"
#                        (user must be logged in)
#   submenu "foo"      = go to submenu "foo"
#   return             = return from submenu
#
# The commands will be done inside the chroot and with the uid and gid
# defined above.
# Parameters to the commands are subject to variable substitution:
#   %r = dglroot, as defined above
#   %n = user nick, if user is logged in
#   %u = shed_uid, as defined above, but numeric
#   %g = game name, if user has selected a game.
#   %s = short game name, if user has selected a game.
#
# eg. commands[login] = mkdir "foo", unlink "bar", setenv "Z" "foo"
#

commands[register] = mkdir "%%CHROOT_TTYRECDIR%%/%n",
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.13/%n.macro",
    ifnxcp "%rdata/crawl-0.13-settings/init.txt" "%rrcfiles/crawl-0.13/%n.rc",
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.14/%n.macro",
    ifnxcp "%rdata/crawl-0.14-settings/init.txt" "%rrcfiles/crawl-0.14/%n.rc",
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.15/%n.macro",
    ifnxcp "%rdata/crawl-0.15-settings/init.txt" "%rrcfiles/crawl-0.15/%n.rc",
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.16/%n.macro",
    ifnxcp "%rdata/crawl-0.16-settings/init.txt" "%rrcfiles/crawl-0.16/%n.rc",
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.17/%n.macro",
    ifnxcp "%rdata/crawl-0.17-settings/init.txt" "%rrcfiles/crawl-0.17/%n.rc",
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.18/%n.macro",
    ifnxcp "%rdata/crawl-0.18-settings/init.txt" "%rrcfiles/crawl-0.18/%n.rc",
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.19/%n.macro",
    ifnxcp "%rdata/crawl-0.19-settings/init.txt" "%rrcfiles/crawl-0.19/%n.rc"


commands[login] = mkdir "%%CHROOT_TTYRECDIR%%/%n",
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.13/%n.macro",
    ifnxcp "%rdata/crawl-0.13-settings/init.txt" "%rrcfiles/crawl-0.13/%n.rc",
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.14/%n.macro",
    ifnxcp "%rdata/crawl-0.14-settings/init.txt" "%rrcfiles/crawl-0.14/%n.rc",
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.15/%n.macro",
    ifnxcp "%rdata/crawl-0.15-settings/init.txt" "%rrcfiles/crawl-0.15/%n.rc",
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.16/%n.macro",
    ifnxcp "%rdata/crawl-0.16-settings/init.txt" "%rrcfiles/crawl-0.16/%n.rc",
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.17/%n.macro",
    ifnxcp "%rdata/crawl-0.17-settings/init.txt" "%rrcfiles/crawl-0.17/%n.rc",
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.18/%n.macro",
    ifnxcp "%rdata/crawl-0.18-settings/init.txt" "%rrcfiles/crawl-0.18/%n.rc",
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.19/%n.macro",
    ifnxcp "%rdata/crawl-0.19-settings/init.txt" "%rrcfiles/crawl-0.19/%n.rc"

# commands[gamestart] =
#     mkdir "%%CHROOT_TTYRECDIR%%/%n",
#     mkdir "%%CHROOT_TTYRECDIR%%/%n/sprint",
#     mkdir "%%CHROOT_TTYRECDIR%%/%n/zotdef",
#     setenv "SIMPLEMAIL" "1",
#     setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n"

menu["mainmenu_anon"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/main_anon.txt"
  # cursor = (5,18)
  commands["lL"] = ask_login
  commands["rR"] = ask_register
  commands["wW"] = watch_menu
  commands["qQ"] = quit
}

menu["mainmenu_admin"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/main_admin.txt"
  commands["cC"] = chpasswd
  commands["eE"] = chmail
  commands["wW"] = watch_menu
  commands["tT"] = submenu "crawl-git"
  commands["9"] = submenu "crawl-0.19"
  commands["8"] = submenu "crawl-0.18"
  commands["7"] = submenu "crawl-0.17"
  commands["6"] = submenu "crawl-0.16"
  commands["5"] = submenu "crawl-0.15"
  commands["4"] = submenu "crawl-0.14"
  commands["3"] = submenu "crawl-0.13"
  commands["pP"] = play_game "dcss-0.19"
  commands["sS"] = play_game "spr-0.19"
  commands["qQ"] = quit
}

menu["mainmenu_user"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/main_user.txt"
  commands["cC"] = chpasswd
  commands["eE"] = chmail
  commands["wW"] = watch_menu
  commands["tT"] = submenu "crawl-git"
  commands["9"] = submenu "crawl-0.19"
  commands["8"] = submenu "crawl-0.18"
  commands["7"] = submenu "crawl-0.17"
  commands["6"] = submenu "crawl-0.16"
  commands["5"] = submenu "crawl-0.15"
  commands["4"] = submenu "crawl-0.14"
  commands["3"] = submenu "crawl-0.13"
  commands["pP"] = play_game "dcss-0.19"
  commands["sS"] = play_game "spr-0.19"
  commands["qQ"] = quit
}

menu["watchmenu_help"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/watchmenu_help.txt"
  commands["qQ "] = return
}

menu["crawl-git"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/crawl_git.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-git/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-git/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-git/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-git/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_GAMEDIR%%/data/docs/crawl_changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_GAMEDIR%%/data/docs/crawl_changelog.txt"
  commands["pP"] = play_game "dcss-git"
  commands["tT"] = play_game "tut-git"
  commands["sS"] = play_game "spr-git"
  commands["xX"] = submenu "experimental"
  commands["A"] = submenu "crawl-git-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return
}

menu["crawl-0.13"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/crawl-0.13.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-0.13/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-0.13/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-0.13/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-0.13/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_BASEDIR%%/crawl-0.13/data/docs/changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_BASEDIR%%/crawl-0.13/data/docs/changelog.txt"
  commands["pP"] = play_game "dcss-0.13"
  commands["tT"] = play_game "tut-0.13"
  commands["sS"] = play_game "spr-0.13"
  commands["zZ"] = play_game "zd-0.13"
  commands["A"] = submenu "crawl-0.13-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return

}

menu["crawl-0.14"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/crawl-0.14.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-0.14/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-0.14/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-0.14/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-0.14/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_BASEDIR%%/crawl-0.14/data/docs/changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_BASEDIR%%/crawl-0.14/data/docs/changelog.txt"
  commands["pP"] = play_game "dcss-0.14"
  commands["tT"] = play_game "tut-0.14"
  commands["sS"] = play_game "spr-0.14"
  commands["zZ"] = play_game "zd-0.14"
  commands["A"] = submenu "crawl-0.14-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return

}

menu["crawl-0.15"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/crawl-0.15.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-0.15/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-0.15/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-0.15/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-0.15/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_BASEDIR%%/crawl-0.15/data/docs/changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_BASEDIR%%/crawl-0.15/data/docs/changelog.txt"
  commands["pP"] = play_game "dcss-0.15"
  commands["tT"] = play_game "tut-0.15"
  commands["sS"] = play_game "spr-0.15"
  commands["zZ"] = play_game "zd-0.15"
  commands["A"] = submenu "crawl-0.15-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return

}

menu["crawl-0.16"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/crawl-0.16.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-0.16/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-0.16/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-0.16/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-0.16/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_BASEDIR%%/crawl-0.16/data/docs/changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_BASEDIR%%/crawl-0.16/data/docs/changelog.txt"
  commands["pP"] = play_game "dcss-0.16"
  commands["tT"] = play_game "tut-0.16"
  commands["sS"] = play_game "spr-0.16"
  commands["A"] = submenu "crawl-0.16-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return

}

menu["crawl-0.17"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/crawl-0.17.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-0.17/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-0.17/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-0.17/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-0.17/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_BASEDIR%%/crawl-0.17/data/docs/changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_BASEDIR%%/crawl-0.17/data/docs/changelog.txt"
  commands["pP"] = play_game "dcss-0.17"
  commands["tT"] = play_game "tut-0.17"
  commands["sS"] = play_game "spr-0.17"
  commands["A"] = submenu "crawl-0.17-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return

}

menu["crawl-0.18"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/crawl-0.18.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-0.18/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-0.18/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-0.18/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-0.18/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_BASEDIR%%/crawl-0.18/data/docs/changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_BASEDIR%%/crawl-0.18/data/docs/changelog.txt"
  commands["pP"] = play_game "dcss-0.18"
  commands["tT"] = play_game "tut-0.18"
  commands["sS"] = play_game "spr-0.18"
  commands["A"] = submenu "crawl-0.18-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return

}

menu["crawl-0.19"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/crawl-0.19.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-0.19/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-0.19/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-0.19/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-0.19/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_BASEDIR%%/crawl-0.19/data/docs/changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_BASEDIR%%/crawl-0.19/data/docs/changelog.txt"
  commands["pP"] = play_game "dcss-0.19"
  commands["tT"] = play_game "tut-0.19"
  commands["sS"] = play_game "spr-0.19"
  commands["A"] = submenu "crawl-0.19-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return

}

menu["experimental"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/experimental.txt"
  commands["qQ"] = return
}

menu["crawl-experimental-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/experimental_adv.txt"
  commands["qQ"] = return
}

menu["crawl-git-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/crawl_git_adv.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-git %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    return
  commands["qQ"] = return
}

menu["crawl-0.13-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/crawl-0.13_adv.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-0.13 %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-0.13-settings/init.txt" "%rrcfiles/crawl-0.13/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.13/%n.macro",
    return
  commands["qQ"] = return
}

menu["crawl-0.14-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/crawl-0.14_adv.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-0.14 %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-0.14-settings/init.txt" "%rrcfiles/crawl-0.14/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.14/%n.macro",
    return
  commands["qQ"] = return
}

menu["crawl-0.15-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/crawl-0.15_adv.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-0.15 %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-0.15-settings/init.txt" "%rrcfiles/crawl-0.15/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.15/%n.macro",
    return
  commands["qQ"] = return
}

menu["crawl-0.16-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/crawl-0.16_adv.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-0.16 %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-0.16-settings/init.txt" "%rrcfiles/crawl-0.16/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.16/%n.macro",
    return
  commands["qQ"] = return
}

menu["crawl-0.17-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/crawl-0.17_adv.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-0.17 %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-0.17-settings/init.txt" "%rrcfiles/crawl-0.17/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.17/%n.macro",
    return
  commands["qQ"] = return
}

menu["crawl-0.18-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/crawl-0.18_adv.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-0.18 %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-0.18-settings/init.txt" "%rrcfiles/crawl-0.18/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.18/%n.macro",
    return
  commands["qQ"] = return
}

menu["crawl-0.19-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/crawl-0.19_adv.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-0.19 %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-0.19-settings/init.txt" "%rrcfiles/crawl-0.19/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.19/%n.macro",
    return
  commands["qQ"] = return
}
##############################################################################
##  GIT
##############################################################################
DEFINE {
  game_path = "/bin/crawl-git-launcher.sh"
  game_name = "Crawl (trunk)"
  short_name = "dcss-git"

  game_args = "/bin/crawl-git-launcher.sh", 
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-git.sock",
              "-morgue", "%rmorgue/%n/"
              
  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-git/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-git-launcher.sh"
  game_name = "Sprint (trunk)"
  short_name = "spr-git"

  game_args = "/bin/crawl-git-launcher.sh", 
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:spr-git.sock",
              "-morgue", "%rmorgue/%n/",
              "-sprint"
              
  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-git-sprint/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-git-launcher.sh"
  game_name = "Tutorial (trunk)"
  short_name = "tut-git"

  game_args = "/bin/crawl-git-launcher.sh", 
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:tut-git.sock",
              "-morgue", "%rmorgue/%n/",
              "-tutorial"
              
  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-git-tut/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
##  0.19
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (0.19)"
  short_name = "dcss-0.19"

  game_args = "/bin/crawl-stable-launcher.sh", "0.19",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.19/%n.rc",
              "-macro", "%rrcfiles/crawl-0.19/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-0.19.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-19/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.19/%n.macro",
    ifnxcp "%rdata/crawl-0.19-settings/init.txt" "%rrcfiles/crawl-0.19/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Sprint (0.19)"
  short_name = "spr-0.19"

  game_args = "/bin/crawl-stable-launcher.sh", "0.19",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.19/%n.rc",
              "-macro", "%rrcfiles/crawl-0.19/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:spr-0.19.sock",
              "-morgue", "%rmorgue/%n/",
              "-sprint"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-19-sprint/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.19/%n.macro",
    ifnxcp "%rdata/crawl-0.19-settings/init.txt" "%rrcfiles/crawl-0.19/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Tutorial (0.19)"
  short_name = "tut-0.19"

  game_args = "/bin/crawl-stable-launcher.sh", "0.19",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.19/%n.rc",
              "-macro", "%rrcfiles/crawl-0.19/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:tut-0.19.sock",
              "-morgue", "%rmorgue/%n/",
              "-tutorial"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-19-tut/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.19/%n.macro",
    ifnxcp "%rdata/crawl-0.19-settings/init.txt" "%rrcfiles/crawl-0.19/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
##  0.18
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (0.18)"
  short_name = "dcss-0.18"

  game_args = "/bin/crawl-stable-launcher.sh", "0.18",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.18/%n.rc",
              "-macro", "%rrcfiles/crawl-0.18/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-0.18.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-18/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.18/%n.macro",
    ifnxcp "%rdata/crawl-0.18-settings/init.txt" "%rrcfiles/crawl-0.18/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Sprint (0.18)"
  short_name = "spr-0.18"

  game_args = "/bin/crawl-stable-launcher.sh", "0.18",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.18/%n.rc",
              "-macro", "%rrcfiles/crawl-0.18/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:spr-0.18.sock",
              "-morgue", "%rmorgue/%n/",
              "-sprint"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-18-sprint/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.18/%n.macro",
    ifnxcp "%rdata/crawl-0.18-settings/init.txt" "%rrcfiles/crawl-0.18/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Tutorial (0.18)"
  short_name = "tut-0.18"

  game_args = "/bin/crawl-stable-launcher.sh", "0.18",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.18/%n.rc",
              "-macro", "%rrcfiles/crawl-0.18/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:tut-0.18.sock",
              "-morgue", "%rmorgue/%n/",
              "-tutorial"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-18-tut/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.18/%n.macro",
    ifnxcp "%rdata/crawl-0.18-settings/init.txt" "%rrcfiles/crawl-0.18/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}
##############################################################################
##  0.17
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (0.17)"
  short_name = "dcss-0.17"

  game_args = "/bin/crawl-stable-launcher.sh", "0.17",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.17/%n.rc",
              "-macro", "%rrcfiles/crawl-0.17/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-0.17.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-17/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.17/%n.macro",
    ifnxcp "%rdata/crawl-0.17-settings/init.txt" "%rrcfiles/crawl-0.17/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Sprint (0.17)"
  short_name = "spr-0.17"

  game_args = "/bin/crawl-stable-launcher.sh", "0.17",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.17/%n.rc",
              "-macro", "%rrcfiles/crawl-0.17/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:spr-0.17.sock",
              "-morgue", "%rmorgue/%n/",
              "-sprint"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-17-sprint/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.17/%n.macro",
    ifnxcp "%rdata/crawl-0.17-settings/init.txt" "%rrcfiles/crawl-0.17/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Tutorial (0.17)"
  short_name = "tut-0.17"

  game_args = "/bin/crawl-stable-launcher.sh", "0.17",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.17/%n.rc",
              "-macro", "%rrcfiles/crawl-0.17/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:tut-0.17.sock",
              "-morgue", "%rmorgue/%n/",
              "-tutorial"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-17-tut/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.17/%n.macro",
    ifnxcp "%rdata/crawl-0.17-settings/init.txt" "%rrcfiles/crawl-0.17/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}
##############################################################################
##  0.16
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (0.16)"
  short_name = "dcss-0.16"

  game_args = "/bin/crawl-stable-launcher.sh", "0.16",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.16/%n.rc",
              "-macro", "%rrcfiles/crawl-0.16/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-0.16.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-16/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.16/%n.macro",
    ifnxcp "%rdata/crawl-0.16-settings/init.txt" "%rrcfiles/crawl-0.16/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Sprint (0.16)"
  short_name = "spr-0.16"

  game_args = "/bin/crawl-stable-launcher.sh", "0.16",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.16/%n.rc",
              "-macro", "%rrcfiles/crawl-0.16/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:spr-0.16.sock",
              "-morgue", "%rmorgue/%n/",
              "-sprint"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-16-sprint/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.16/%n.macro",
    ifnxcp "%rdata/crawl-0.16-settings/init.txt" "%rrcfiles/crawl-0.16/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Tutorial (0.16)"
  short_name = "tut-0.16"

  game_args = "/bin/crawl-stable-launcher.sh", "0.16",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.16/%n.rc",
              "-macro", "%rrcfiles/crawl-0.16/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:tut-0.16.sock",
              "-morgue", "%rmorgue/%n/",
              "-tutorial"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-16-tut/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.16/%n.macro",
    ifnxcp "%rdata/crawl-0.16-settings/init.txt" "%rrcfiles/crawl-0.16/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}
##############################################################################
##  0.15
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (0.15)"
  short_name = "dcss-0.15"

  game_args = "/bin/crawl-stable-launcher.sh", "0.15",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.15/%n.rc",
              "-macro", "%rrcfiles/crawl-0.15/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-0.15.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-15/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.15/%n.macro",
    ifnxcp "%rdata/crawl-0.15-settings/init.txt" "%rrcfiles/crawl-0.15/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Sprint (0.15)"
  short_name = "spr-0.15"

  game_args = "/bin/crawl-stable-launcher.sh", "0.15",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.15/%n.rc",
              "-macro", "%rrcfiles/crawl-0.15/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:spr-0.15.sock",
              "-morgue", "%rmorgue/%n/",
              "-sprint"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-15-sprint/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.15/%n.macro",
    ifnxcp "%rdata/crawl-0.15-settings/init.txt" "%rrcfiles/crawl-0.15/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Zot Defence (0.15)"
  short_name = "zd-0.15"

  game_args = "/bin/crawl-stable-launcher.sh", "0.15",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.15/%n.rc",
              "-macro", "%rrcfiles/crawl-0.15/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:zd-0.15.sock",
              "-morgue", "%rmorgue/%n/",
              "-zotdef"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-15-zotdef/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.15/%n.macro",
    ifnxcp "%rdata/crawl-0.15-settings/init.txt" "%rrcfiles/crawl-0.15/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Tutorial (0.15)"
  short_name = "tut-0.15"

  game_args = "/bin/crawl-stable-launcher.sh", "0.15",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.15/%n.rc",
              "-macro", "%rrcfiles/crawl-0.15/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:tut-0.15.sock",
              "-morgue", "%rmorgue/%n/",
              "-tutorial"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-15-tut/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.15/%n.macro",
    ifnxcp "%rdata/crawl-0.15-settings/init.txt" "%rrcfiles/crawl-0.15/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
##  0.14
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (0.14)"
  short_name = "dcss-0.14"

  game_args = "/bin/crawl-stable-launcher.sh", "0.14",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.14/%n.rc",
              "-macro", "%rrcfiles/crawl-0.14/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-0.14.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-14/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.14/%n.macro",
    ifnxcp "%rdata/crawl-0.14-settings/init.txt" "%rrcfiles/crawl-0.14/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Sprint (0.14)"
  short_name = "spr-0.14"

  game_args = "/bin/crawl-stable-launcher.sh", "0.14",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.14/%n.rc",
              "-macro", "%rrcfiles/crawl-0.14/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:spr-0.14.sock",
              "-morgue", "%rmorgue/%n/",
              "-sprint"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-14-sprint/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.14/%n.macro",
    ifnxcp "%rdata/crawl-0.14-settings/init.txt" "%rrcfiles/crawl-0.14/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Zot Defence (0.14)"
  short_name = "zd-0.14"

  game_args = "/bin/crawl-stable-launcher.sh", "0.14",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.14/%n.rc",
              "-macro", "%rrcfiles/crawl-0.14/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:zd-0.14.sock",
              "-morgue", "%rmorgue/%n/",
              "-zotdef"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-14-zotdef/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.14/%n.macro",
    ifnxcp "%rdata/crawl-0.14-settings/init.txt" "%rrcfiles/crawl-0.14/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Tutorial (0.14)"
  short_name = "tut-0.14"

  game_args = "/bin/crawl-stable-launcher.sh", "0.14",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.14/%n.rc",
              "-macro", "%rrcfiles/crawl-0.14/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:tut-0.14.sock",
              "-morgue", "%rmorgue/%n/",
              "-tutorial"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-14-tut/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.14/%n.macro",
    ifnxcp "%rdata/crawl-0.14-settings/init.txt" "%rrcfiles/crawl-0.14/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}



##############################################################################
##  0.13
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (0.13)"
  short_name = "dcss-0.13"

  game_args = "/bin/crawl-stable-launcher.sh", "0.13",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.13/%n.rc",
              "-macro", "%rrcfiles/crawl-0.13/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-0.13.sock",
              "-morgue", "%rmorgue/%n/"
              
  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-13/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.13/%n.macro",
    ifnxcp "%rdata/crawl-0.13-settings/init.txt" "%rrcfiles/crawl-0.13/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Sprint (0.13)"
  short_name = "spr-0.13"

  game_args = "/bin/crawl-stable-launcher.sh", "0.13",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.13/%n.rc",
              "-macro", "%rrcfiles/crawl-0.13/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:spr-0.13.sock",
              "-morgue", "%rmorgue/%n/",
              "-sprint"
              
  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-13-sprint/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.13/%n.macro",
    ifnxcp "%rdata/crawl-0.13-settings/init.txt" "%rrcfiles/crawl-0.13/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Zot Defence (0.13)"
  short_name = "zd-0.13"

  game_args = "/bin/crawl-stable-launcher.sh", "0.13",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.13/%n.rc",
              "-macro", "%rrcfiles/crawl-0.13/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:zd-0.13.sock",
              "-morgue", "%rmorgue/%n/",
              "-zotdef"
              
  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-13-zotdef/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.13/%n.macro",
    ifnxcp "%rdata/crawl-0.13-settings/init.txt" "%rrcfiles/crawl-0.13/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Tutorial (0.13)"
  short_name = "tut-0.13"

  game_args = "/bin/crawl-stable-launcher.sh", "0.13",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.13/%n.rc",
              "-macro", "%rrcfiles/crawl-0.13/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:tut-0.13.sock",
              "-morgue", "%rmorgue/%n/",
              "-tutorial"
              
  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-13-tut/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.13/%n.macro",
    ifnxcp "%rdata/crawl-0.13-settings/init.txt" "%rrcfiles/crawl-0.13/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}



##############################################################################
##  Other branches
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (nostalgia)"
  short_name = "dcss-nostalgia"

  game_args = "/bin/crawl-stable-launcher.sh", "nostalgia",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-nostalgia.sock",
              "-morgue", "%rmorgue/%n/"
              
  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-nostalgia/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}


DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (salamander)"
  short_name = "dcss-salamander"

  game_args = "/bin/crawl-stable-launcher.sh", "salamander",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-salamander.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-salamander/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}


DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (faithful)"
  short_name = "dcss-faithful"

  game_args = "/bin/crawl-stable-launcher.sh", "faithful",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-faithful.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-faithful/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}


DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (no_backtracking_god)"
  short_name = "dcss-no_backtracking_god"

  game_args = "/bin/crawl-stable-launcher.sh", "no_backtracking_god",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-no_backtracking_god.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-no_backtracking_god/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (evoker-god)"
  short_name = "dcss-evoker-god"

  game_args = "/bin/crawl-stable-launcher.sh", "evoker-god",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-evoker-god.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-evoker-god/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (imp)"
  short_name = "dcss-imp"

  game_args = "/bin/crawl-stable-launcher.sh", "imp",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-imp.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-imp/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (bearkin)"
  short_name = "dcss-bearkin"

  game_args = "/bin/crawl-stable-launcher.sh", "bearkin",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-bearkin.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-bearkin/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (squarelos-0.17)"
  short_name = "dcss-squarelos-0.17"

  game_args = "/bin/crawl-stable-launcher.sh", "squarelos-0.17",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-squarelos-0.17.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-squarelos-0.17/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}


DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (faithful)"
  short_name = "dcss-faithful"

  game_args = "/bin/crawl-stable-launcher.sh", "faithful",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-faithful.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-faithful/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}


DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (combo_god)"
  short_name = "dcss-combo_god"

  game_args = "/bin/crawl-stable-launcher.sh", "combo_god",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-combo_god.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-combo_god/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (adrenaline_rush)"
  short_name = "dcss-adrenaline_rush"

  game_args = "/bin/crawl-stable-launcher.sh", "adrenaline_rush",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-adrenaline_rush.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-adrenaline_rush/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (basajaunak)"
  short_name = "dcss-basajaun"

  game_args = "/bin/crawl-stable-launcher.sh", "basajaun",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-basajaun.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-basajaun/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (thorn_god)"
  short_name = "dcss-thorn_god"

  game_args = "/bin/crawl-stable-launcher.sh", "thorn_god",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-thorn_god.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-thorn_god/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (abyssrun)"
  short_name = "dcss-abyssrun"

  game_args = "/bin/crawl-stable-launcher.sh", "abyssrun",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-abyssrun.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-abyssrun/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (mulch_ado_about_nothing)"
  short_name = "dcss-mulch_ado_about_nothing"

  game_args = "/bin/crawl-stable-launcher.sh", "mulch_ado_about_nothing",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-mulch_ado_about_nothing.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-mulch_ado_about_nothing/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}
